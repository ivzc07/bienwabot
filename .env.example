# rebe-agent environment.
#
# Every variable the process reads, and nothing else - `rebe_agent/config.py` is
# the single entry point, so this file and that module stay in step.
# In production these are Coolify per-app environment variables, encrypted at
# rest. Never commit real values; this file carries names and shapes only.
#
# Source of truth: docs/wayfinder/deployment-architecture-spec.md, section 4.

# --- Required. The process refuses to start if any of these is absent or blank.

# DeepSeek brain (non-thinking chat model).
DEEPSEEK_API_KEY=

# Internal URL of the bien-evo Evolution API. Container name, not a public host:
# the agent reaches Evolution over the shared Docker network and has no public URL.
# The name below is the real container on this server, recorded in
# docs/wayfinder/bien-evo-provisioning.md; it only resolves if the agent is
# deployed to destination pxrf9uiha61v0zcdq73g5wnk, like every other resource here.
EVOLUTION_API_URL=http://api-i856ku5lxcr2o1v64eveeahq:8080

# Evolution's AUTHENTICATION_API_KEY.
EVOLUTION_API_KEY=

# The WhatsApp group Rebe posts into, as a JID (`<id>@g.us`). The scheduler leg
# needs it at boot: a bot with nowhere to post is a misconfigured bot, not an idle one.
REBE_GROUP_JID=

# Token embedded in the webhook path (POST /webhook/<secret>). Defence in depth
# even on an internal network; must match the per-instance webhook Evolution posts to.
# The agent listens on port 8000 inside the container and publishes nothing, so
# the per-instance webhook URL is http://rebe-agent:8000/webhook/<this value>.
WEBHOOK_SECRET=

# psycopg URL to the `rebe` database on bien-evo's Postgres (deepseek_usage, sends,
# and the group_memory / posted_store tables that later tickets add).
REBE_DATABASE_URL=postgresql://rebe:CHANGE_ME@postgres-i856ku5lxcr2o1v64eveeahq:5432/rebe

# Out-of-band ops channel: ban / 463 / Evolution disconnect / DeepSeek errors go
# out here, and the soft pause comes back in (/pausa, /reanuda, /estado).
# Telegram rather than WhatsApp, so alerts still arrive when Evolution is down.
# TELEGRAM_CHAT_ID is the only chat whose commands are obeyed; anything from
# anywhere else is ignored without an answer.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Uptime Kuma push monitor. The agent pushes from inside its own loop about every
# 60s, so a hung loop stops the beat as surely as a dead process does, and the
# missed beat is what makes Kuma fire the Telegram alert.
KUMA_PUSH_URL=http://kuma:3001/api/push/CHANGE_ME

# --- Optional. Defaults shown; set only to override.

# Active Evolution instance. Manual failover on a permanent ban is: set this to
# bien-backup and redeploy. Never run both instances posting at once.
EVOLUTION_INSTANCE=bien-rebe

# DeepSeek's OpenAI-compatible endpoint. Override only to point the agent at a
# stand-in during testing; the model ID itself is pinned in rebe_agent/brain.py
# because non-thinking is a correctness decision, not an operator setting.
DEEPSEEK_BASE_URL=https://api.deepseek.com

# Drives the pacer's quiet hours - the 23:00-08:00 hold on scheduled posts and the
# 02:00-06:00 near-silence - the daily send ceiling, and the circadian posting shape.
TZ=America/Mexico_City

# CRITICAL | ERROR | WARNING | INFO | DEBUG
LOG_LEVEL=INFO

# Experimental dense posting. Leave blank = shipped cadence (~4 weekday / ~2 weekend).
# Set to 30 to try a slot every half hour from 08:00-23:00 local. WhatsApp may
# treat that rate as automation - test briefly, then clear. Already-drawn days
# in `planned_slots` keep the old shape until the next dawn roll (or the rows
# for today are deleted).
CADENCE_SLOT_MINUTES=
